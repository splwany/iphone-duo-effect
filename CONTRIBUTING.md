# 贡献与发布

当前采用 Vite + React + TypeScript。以 `src/` 为运行源码，`public/` 存放静态资源，逐次修改一个视觉变量；不要同时改动全屏布局、感应方向和模糊模型。

提交前运行 README 中的检查，再用 HANDOFF 中的 iPhone 真机清单回归。描述修改前后、参数、角度、测试设备与限制。桌面浏览器模拟不能替代主屏幕体感验证。

修改参数范围时，保留版本化的 localStorage 迁移；不要让同一百分比静默对应完全不同的效果。调整默认值时同步更新渲染器默认、控件绑定、页面帮助与文档。

首次发布前：选择许可证与著作权归属；核对素材权利；确认只发布此包内容；不要把本地证书、截图或源工作区 Git 历史一并提交。不要维护重复的完整源码快照；以 Git 记录变更。
